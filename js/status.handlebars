{{#each this}}
<div class="flex course" id="{{ouNumber}}-OU">
    <img src="https://localhost:8000/resources/x.ico" class="deleteCourseButton" id="delete{{ouNumber}}" />
    <span class="course-title">{{statusName}}</span>
    <span class="spacer"></span>
    <span class="status-{{status}}">{{status}}</span>
</div>
{{/each}}
